[pytest]
pythonpath = . src
testpaths = tests
markers =
    integration: integration test — requires real models; pass --integration to run
    performance: performance benchmark — requires real models; pass --performance to run
